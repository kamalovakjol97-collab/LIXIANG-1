# ООО «ХЖЛ» — Корпоративный сайт и личный кабинет

Корпоративный сайт и личный кабинет для логистической компании ООО «ХЖЛ». Система включает публичную часть сайта и полнофункциональный личный кабинет для клиентов и менеджеров.

## 🚀 Технологии

- **Next.js 14** (App Router)
- **TypeScript**
- **Tailwind CSS**
- **Prisma** (ORM)
- **SQLite** (база данных для разработки)
- **Framer Motion** (анимации)
- **Lucide React** (иконки)
- **bcryptjs** (хеширование паролей)

## 📁 Структура проекта

```
/
├── app/
│   ├── api/                    # API роуты
│   │   ├── auth/               # Аутентификация
│   │   ├── orders/             # Заказы
│   │   └── clients/            # Клиенты
│   ├── lk/                     # Личный кабинет
│   │   ├── login/              # Страница входа
│   │   ├── client/             # ЛК клиента
│   │   └── manager/           # ЛК менеджера
│   ├── about/                  # О компании
│   ├── services/              # Услуги
│   ├── contacts/               # Контакты
│   ├── layout.tsx              # Корневой layout
│   ├── page.tsx                # Главная страница
│   └── globals.css             # Глобальные стили
├── components/
│   ├── Header.tsx              # Хедер с навигацией
│   ├── Footer.tsx              # Футер
│   ├── ContactForm.tsx         # Форма заявки
│   ├── OrderChat.tsx           # Чат по заказу
│   ├── OrderDocuments.tsx      # Документы заказа
│   └── ...                     # Другие компоненты
├── lib/
│   ├── prisma.ts               # Prisma клиент
│   └── auth.ts                 # Утилиты аутентификации
├── prisma/
│   ├── schema.prisma           # Схема базы данных
│   └── migrations/             # Миграции БД
└── package.json
```

## 🛠️ Установка и запуск

### 1. Установка зависимостей

```bash
npm install
```

### 2. Настройка базы данных

База данных SQLite создаётся автоматически при первом запуске. Для продакшена можно использовать PostgreSQL или MySQL.

```bash
# Генерация Prisma клиента
npx prisma generate

# Применение миграций (уже выполнено)
npx prisma migrate dev
```

### 3. Запуск в режиме разработки

```bash
npm run dev
```

Откройте [http://localhost:3000](http://localhost:3000) в браузере.

### 4. Создание первого менеджера

Для создания первого менеджера используйте API:

```bash
curl -X POST http://localhost:3000/api/auth/register \
  -H "Content-Type: application/json" \
  -d '{
    "email": "manager@example.com",
    "password": "password123",
    "name": "Менеджер",
    "role": "manager"
  }'
```

Или зарегистрируйтесь через форму на сайте и измените роль в базе данных.

### 5. Сборка для продакшена

```bash
npm run build
npm start
```

## ✨ Функциональность

### Публичная часть

- ✅ Главная страница с описанием компании
- ✅ Страница "О компании"
- ✅ Страница "Услуги"
- ✅ Страница "Контакты" с формой обратной связи
- ✅ Форма заявки (создаёт заказ и пользователя)
- ✅ Загрузка файлов при создании заявки (инвойс, упаковочный лист и т.д.)
- ✅ Адаптивный дизайн

### Личный кабинет клиента

- ✅ Список заказов клиента
- ✅ Карточка заказа с детальной информацией
- ✅ История изменения статусов
- ✅ Чат по заказу с менеджером
- ✅ Просмотр и скачивание документов

### Личный кабинет менеджера

- ✅ Список всех заказов с фильтрацией
- ✅ Поиск по номеру заказа, клиенту, маршруту
- ✅ Редактирование заказа
- ✅ Изменение статуса заказа с комментариями
- ✅ Чат с клиентом по заказу
- ✅ Загрузка документов для заказа
- ✅ Управление клиентами
- ✅ Просмотр истории заказов клиента

### Статусы заказов (фиксированная цепочка)

1. **Заявка создана** — заявка только что создана клиентом
2. **В работе** — заявка обрабатывается менеджером
3. **Документы получены** — все необходимые документы получены
4. **Груз отправлен** — груз отправлен в путь
5. **Груз прибыл** — груз прибыл в пункт назначения
6. **Завершено** — заказ полностью завершён

Менеджер может добавлять комментарии к каждому изменению статуса.

## 🗄️ База данных

Схема базы данных включает следующие таблицы:

- `users` — пользователи (клиенты и менеджеры)
- `orders` — заказы
- `order_status_history` — история изменения статусов (с комментариями)
- `messages` — сообщения в чате по заказу
- `documents` — документы заказов

## 🔐 Аутентификация

Текущая реализация использует localStorage для хранения данных пользователя. Для продакшена рекомендуется:

- Использовать сессии (NextAuth.js)
- Или JWT токены с httpOnly cookies
- Добавить защиту API роутов

## 📝 API Роуты

### Аутентификация

- `POST /api/auth/register` — регистрация пользователя
- `POST /api/auth/login` — вход пользователя

### Заказы

- `GET /api/orders` — список заказов (с фильтрами)
- `POST /api/orders` — создание заказа
- `GET /api/orders/[id]` — получение заказа
- `PATCH /api/orders/[id]` — обновление заказа
- `GET /api/orders/[id]/messages` — сообщения по заказу
- `POST /api/orders/[id]/messages` — отправка сообщения
- `GET /api/orders/[id]/documents` — документы заказа
- `POST /api/orders/[id]/documents` — загрузка документа

### Клиенты

- `GET /api/clients` — список клиентов (только для менеджеров)

## 🎨 Цветовая палитра

- **Основной цвет**: `#0B3C5D` (синий)
- **Акцент (оранжевый)**: `#F97316`
- **Акцент (зелёный)**: `#10B981`
- **Фон**: белый / серые оттенки

## 📝 Примечания

### Для продакшена

1. Замените SQLite на PostgreSQL или MySQL
2. Настройте переменные окружения в `.env`
3. Добавьте защиту API роутов (middleware для проверки ролей)
4. Настройте хранение файлов (S3, локальное хранилище)
5. Добавьте систему сессий (NextAuth.js)
6. Настройте HTTPS
7. Добавьте логирование и мониторинг

### Безопасность

- Пароли хешируются с помощью bcrypt
- В продакшене добавьте rate limiting
- Валидация всех входных данных
- Защита от SQL-инъекций (Prisma)
- Защита от XSS (React автоматически экранирует)

## 📄 Лицензия

© 2024 ООО «ХЖЛ». Все права защищены.

## 👥 Разработка

Проект разработан согласно техническому заданию с фокусом на функциональность и простоту использования.
